package code

import org.assertj.core.api.Assertions
import org.junit.jupiter.api.Test

class CodeTest {
    /*@Test
    fun parameterTest() {
        // given
        val codeChecker: CodeChecker = CodeChecker.rules()
            .limitParameters(2)
            .build()

        // when
        val check: Boolean = codeChecker.checkParameterCount("pairmatching", true)

        // then
        Assertions.assertThat(check).`as`(codeChecker.getMessage()).isEqualTo(true)
    }*/
}